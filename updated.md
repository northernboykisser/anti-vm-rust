
# ANTIVM UPDATE

V1.1.0 at 11-08-25
Added: / Добавлено:



```javascript
ProtectionBuilder::new()
    .filtered(|| {
        // вместо закрытия — своя логика
        // custom logic instead of closing
    })
    .init();
```

